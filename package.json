{
  "name": "agent-secure",
  "version": "1.0.0",
  "description": "Tri-model security orchestrator: a Flow-Scan and Importance-Scan model gate every request a worker AI agent makes on your machine.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "express": "^4.21.2"
  }
}
